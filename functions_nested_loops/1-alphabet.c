#include <stdio.h>
#include "main.h"
/**
 * main - Empty point
 * Description: 'the program's description'
 * Return: Always 0 (Success)
 */
int main(void)
{
	print_alphabet(x);

	for (x = 'a'; x <= 'z'; x++)
	{
		_putchar(x);
	}
	_putchar('\n');
	return (0);
}
