#include <stdio.h>
#include "main.h"
/**
 * main - Empty point
 * Description: 'the program's description'
 * Return: Always 0 (Success)
 */

int print_sign(int n)
{
	for (c = 'a'; c <= 'z'; c++)
	{
		_putchar(c);
		if (islower(c) != c)
		{
			return(0)
		}
		else
		{
			return(0);
		}
	}

}
