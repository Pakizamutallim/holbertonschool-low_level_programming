#include <stdio.h>
#include "main.h"
/**
 * main - Empty point
 * Description: 'the program's description'
 * Return: Always 0 (Success)
 */

int print_sign(int n)
{
	for (n = 'a'; n <= 'z'; n++)
	{
		_putchar(n);

		if (islower(c) != 0)
		{
			return(0);
		}
		else
		{
			return(0);
		}
	}
}
