#include <stdio.h>
#include "main.h"
/**
 * main - Empty point
 * Description: 'the program's description'
 * Return: Always 0 (Success)
 */

int _islower(int c)
{
	for (c = 'a'; c <= 'z'; c++)
	{
		_putchar(c);

		if (islower(c) != 0)
		{
			return(0);
		}
		else
		{
			return(0);
		}
	}
}
