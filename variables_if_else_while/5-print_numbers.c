#include <stdio.h>
#include <math.h>
/**
 * main - Empty point
 * Description: 'the program's description'
 * Return: Always 0 (Success)
 */

int main(void)
{
	int x;
	for (x = '0'; x < '10'; x++)
	{
		putchar(x);
	}
	return (0);
}
