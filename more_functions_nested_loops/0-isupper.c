#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * _isupper - check the code.
 * @c - is a letter.
 * Return: Always 0.
 */

int _isupper(int c)
{
	if (x>=65 && x <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
