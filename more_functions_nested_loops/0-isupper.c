#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * _isupper - check the code.
 * @c - is a letter.
 * Return: Always 0.
 */

int _isupper(int c)
{
	if (_isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
